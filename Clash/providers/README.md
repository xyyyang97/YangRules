# Providers Cache Directory

This directory is intentionally kept almost empty in Git.

`Yang.yaml` references remote rule-providers from the BlackMatrix7 project and
stores downloaded provider caches locally under `./providers/*.yaml` on the
client side.

This repository does not vendor upstream domain lists.
